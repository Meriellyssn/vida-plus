/**
 * Arquivo de Declaração de Tipos do Vite (vite-env.d.ts)
 *
 * Este arquivo é um arquivo de declaração de tipos do TypeScript (.d.ts).
 * Sua principal função é informar ao TypeScript sobre os tipos específicos
 * do ambiente do Vite que estão disponíveis no lado do cliente (no navegador).
 *
 * Funcionalidades principais:
 * - Garante que o TypeScript entenda funcionalidades do Vite como:
 * - Variáveis de ambiente (`import.meta.env`).
 * - API de Hot Module Replacement (HMR) (`import.meta.hot`).
 * - Importação de arquivos estáticos (ex: `import logo from './logo.svg'`).
 *
 * @author Meirielli S. Sousa do N. 
 * @version 1.0.0
 * @since 2025
 */

/// <reference types="vite/client" />
